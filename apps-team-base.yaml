apiVersion: kustomize.toolkit.fluxcd.io/v1beta1
kind: Kustomization
metadata:
  name: app-team-base
  namespace: flux-system
spec:
  interval: 60ss
  sourceRef:
    kind: GitRepository
    name: fluxcd
    namespace: flux-system
  path: ./apps/team-base
  prune: true
  wait: true